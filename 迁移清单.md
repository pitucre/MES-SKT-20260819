# 盘点自动移库功能 — 生产迁移确认清单

功能说明：PDA 盘点（初盘 `MobileApp/WarehouseCheck.aspx`、复盘 `MobileApp/WarehouseCheckReplay.aspx`）时，实际库位与系统库位不一致自动处理。按配置 911 切换：`'1'`=当场执行移库/记录（仅同仓当场执行，跨仓记录差异）；`'2'`=只记录实际库位到盘点明细，平帐时统一处理（同仓移库/跨仓调拨入库）。物料与成品均支持。

---

## 一、数据库变更

### 1. 盘点明细表加列

```sql
ALTER TABLE dbo.Prod_WarehouseCheckOrderDtl ADD RealBarCode varchar(50) NULL;
```

> 目的：记录实际库位，供平帐统一处理。

### 2. 配置 911（盘点移库处理方式开关）

```sql
UPDATE dbo.Prod_MaterialSysConfig
SET ConfigDesc='1-当场移库(同仓)/记录(跨仓) 2-只记录',
    ConfigResult='1',
    Remark='盘点时实际库位与系统库位不一致的处理方式:1-当场执行移库/记录(仅同仓当场执行,跨仓记录差异);2-只记录实际库位到盘点明细,平帐时统一处理(同仓移库/跨仓调拨入库)'
WHERE ConfigTypeId=911;
```

> 注意：
> - 若生产该配置行不存在，需 INSERT（ConfigTypeId=911, ConfigResult='1'）。
> - **测试库当前值：`'1'`**（当场移库同仓/记录跨仓）。
> - 生产默认建议 `'2'`（只记录，平帐统一处理，风险最低）。
> - ConfigDesc 仅 50 字符，完整说明放 Remark。

### 3. 调拨单号规则 -25（跨仓调拨入库生成单号用）

```sql
INSERT INTO dbo.Basal_SerialNumber (Next_Number_Type, Apply_Type, Type_Value, Revision, Prefix, Suffix, Description)
VALUES (-25, 1, 'ALL', NULL, 'Tra%YEAR%%MONTH%', '', '盘点平帐调拨入库单号');
```

> 若生产已存在 `Next_Number_Type=-25` 则跳过。测试库已建（SerialNumberID=24）。

### 4. 存储过程（6 个）

| # | SP | 操作 | 说明 |
|---|----|------|------|
| 1 | `uspStorageTransfer` | ALTER | 物料库位转移状态校验放宽：`Status NOT IN (0,14)`（放行盘点锁定物料） |
| 2 | `uspWarehouseCheckMoveMaterial` | ALTER | 新签名 `(cposcode, grn, checkNo, userName, result OUTPUT)`；物料/成品 + 同仓/跨仓 + 配置911分流 |
| 3 | `uspWarehouseCheckTransferIn` | CREATE | 跨仓调拨入库：生成 Tra 单号 → 建单头/明细/GRN绑定 → `uspSaveTransferIn` |
| 4 | `uspWarehouseCheckHandleLocationDiff_Program` | CREATE | 平帐统一处理：遍历盘点明细(RealBarCode差异+NowQty>0)，**同仓/跨仓均直接移库**（不生成调拨单），记录移库历史 ActionType=43 |
| 5 | `uspSaveCheckOrder` | ALTER | 复盘回写SELECT改为按GRN明细返回：旧仓库编码+新仓库编码+实盘数量（支持U9跨仓盘点回写） |
| 6 | `uspGetCheckOrderDetail` | ALTER | 去掉 `CAST(A.StockQty AS INT)` 截断问题，直接返回原始decimal值 |

> 注意：
> - 脚本用 **带 BOM 的 UTF-8** 保存后执行，否则中文注释在 sqlcmd 下会静默失败。
> - 新建SP统一加 `_Program` 后缀，改原有SP前先备份（`EXEC usp_DBA_BackupProc_Program`）。

### 5. SP备份清单

| 原SP | 备份名 | 备份时间 |
|------|--------|----------|
| uspSaveCheckOrder | uspSaveCheckOrder_BAK_20260827 | 2026-08-27 |
| uspGetCheckOrderDetail | uspGetCheckOrderDetail_BAK_20260827 | 2026-08-27 |
| uspWarehouseCheckGetMaList | uspWarehouseCheckGetMaList_BAK_20260827 | 2026-08-27 |
| uspWarehouseCheckDifferenceList | uspWarehouseCheckDifferenceList_BAK_20260827 | 2026-08-27 |
| uspWarehouseCheckHandleLocationDiff_Program | uspWarehouseCheckHandleLocationDiff_Program_BAK_20260827 | 2026-08-27 |

---

## 二、代码变更（编译部署）

### 1. DLL（需要重新编译并替换）

| DLL | 修改点 |
|-----|--------|
| `SKT.LeanMES.Warehouse.BLL.dll` | `WarehouseLocation.WarehouseCheckMoveMaterial` 增加 `checkNo` 参数并传给 SP |
| `SKT.LeanMES.Material.BLL.dll` | `WarehouseCheck.SaveCheckOrder`：`Flag=2` 平帐后、同一事务内调 `uspWarehouseCheckHandleLocationDiff` |
| `SKT.LeanMES.Web.dll` | `AjaxWarehouseCheck.MoveMaterial` 增加 `checkNo` 参数并透传 |

编译命令（v4.8）：

```powershell
$fw="C:\Program Files (x86)\Reference Assemblies\Microsoft\Framework\.NETFramework\v4.8"
& "C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" "DLL\LeanMES.8.5.7.DLL.sln" /t:Build "/p:TargetFrameworkVersion=v4.8" "/p:FrameworkPathOverride=$fw" /v:minimal /nologo
& "C:\Program Files\Microsoft Visual Studio\18\Community\MSBuild\Current\Bin\MSBuild.exe" "Code\Web\Web.csproj" /t:Build "/p:TargetFrameworkVersion=v4.8" "/p:FrameworkPathOverride=$fw" /v:minimal /nologo
```

部署：将以上 3 个 DLL 复制到 `Code\Lib` 与 `Code\Web\bin`。

### 2. 页面（源码更新，随 Web 一起发布）

| 页面 | 修改点 |
|------|--------|
| `MobileApp/WarehouseCheck.aspx`（初盘） | MoveMaterial 回调改为 3 参数 `(realBarCode, GRN, CheckListNo)`；提示语改为显示 SP 返回消息；库位下拉读 `e.WhBarcode` |
| `MobileApp/WarehouseCheckReplay.aspx`（复盘） | 同上；库位下拉读 `e.BarCode`；加"认可初盘结果"按钮 |
| `Handler/WarehouseCheck.ashx.cs` | `GetWarehouseCheckListInfo` 过滤条件：type=1→状态2(待初盘)，type=2→状态3(待复盘) |
| `DLL/ERP/WriteBackERP.cs` | InventoryList回写适配新SP返回字段（旧仓库+新仓库） |

---

## 三、部署后验证步骤

1. 确认 `RealBarCode` 列、4 个 SP、配置 911、单号规则 -25 均存在。
2. 用 **32 位** IIS 应用池/进程重启站点（64 位会报 `sapnco` BadImageFormatException）。
3. PDA 初盘/复盘：扫描测试物料，实际库位输入不同库位，验证：
   - 配置911='1' 同仓 → 当场移库；跨仓 → 只记录。
   - 配置911='2' → 只记录 RealBarCode。
4. 平帐：确认同仓移库、跨仓生成 Tra 调拨单并入库。

---

## 四、关键设计决策

1. **跨仓盘点不生成调拨单**：用户要求即使跨库也只做移库操作，通过盘点单回写ERP调整库存。`uspWarehouseCheckHandleLocationDiff_Program` 直接更新 `Prod_MaterialUnit` 的 `cBarCode` 和 `WarehouseId`。
2. **回写SELECT按GRN明细返回**：`uspSaveCheckOrder` 复盘回写改为返回每个GRN的旧仓库+新仓库，让U9正确处理跨仓盘点。
3. **"以实物为准"原则**：GRN不存在/GRN不在库时跳过物理移库、仅记录实际库位到盘点明细。
4. **库位下拉数据来源**：初盘读 `e.WhBarcode`，复盘读 `e.BarCode`（因SP返回字段不同）。

---

## 五、待确认事项

- **生产环境 .NET Framework 版本**：v4.8 编译的 DLL 需生产为 v4.8（或兼容）。请确认生产服务器 .NET 版本。
- **配置 911 生产初始值**：选 `'1'`（当场执行）还是 `'2'`（只记录、平帐统一处理）。

---

## 六、测试记录（测试库 LeanMes_Test 已通过）

| 场景 | 配置911 | 结果 |
|------|--------|------|
| 同仓 当场 | '1' | 物料 A2-2→A2-3，状态保持 14，通过 |
| 同仓 只记录 | '2' | RealBarCode=A2-3，物料不动，通过 |
| 跨仓 当场 | '1' | RealBarCode=1044，物料不动（仅记录），通过 |
| 跨仓盘点回写 | - | uspSaveCheckOrder返回旧仓库+新仓库，U9正确处理 |
| GRN250530000084显示2个GRN | - | uspWarehouseCheckGetMaList修复：扫箱内成员仅返回扫描的GRN |
| GRN251209000001显示0 | - | uspGetCheckOrderDetail修复：去掉CAST(INT)截断 |

测试物料：GRN251216000003（MaterialUnitId=289449），盘点单 PDD260106001，库位 A2-2→A2-3，WarehouseId=7。测试完成后已还原数据。

**跨仓测试盘点单 PDD2026082701（已清理）**：
| GRN | 仓库 | 库位 | 系统数量 | 类型 |
|-----|------|------|----------|------|
| GRN241031000003 | 原料库(7) | A14-2-L | 25 | 跨仓 |
| GRN250108000017 | 委外-彩盒(19) | LSO-2 | 238 | 跨仓 |
| GRN241029000006 | 委外线束-注塑(3) | LS-1 | 1000 | 跨仓 |
| GRN241031000002 | 原料库(7) | B9-4-L | 100 | 同仓 |
| GRN241106000021 | 原料库(7) | B7-4-R | 362 | 同仓 |
